import type { NextPage } from 'next';
import { Box, Typography } from '@mui/material';

const Home: NextPage = () => {
  return (
    <Box>
      <Typography variant="h4">Welcome to hAIper Platform</Typography>
    </Box>
  );
};

export default Home;
