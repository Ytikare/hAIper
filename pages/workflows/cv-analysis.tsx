import type { NextPage } from 'next';
import { Box, Typography } from '@mui/material';

const CVAnalysis: NextPage = () => {
  return (
    <Box>
      <Typography variant="h4">CV Analysis Workflow</Typography>
    </Box>
  );
};

export default CVAnalysis;
