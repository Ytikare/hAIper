import type { NextPage } from 'next';
import { Box, Typography } from '@mui/material';

const ITHelper: NextPage = () => {
  return (
    <Box>
      <Typography variant="h4">IT Helper Workflow</Typography>
    </Box>
  );
};

export default ITHelper;
