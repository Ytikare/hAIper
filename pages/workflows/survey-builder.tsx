import type { NextPage } from 'next';
import { Box, Typography } from '@mui/material';

const SurveyBuilder: NextPage = () => {
  return (
    <Box>
      <Typography variant="h4">Survey Builder Workflow</Typography>
    </Box>
  );
};

export default SurveyBuilder;
